You are one iteration of an unattended overnight build. The user (Oliver) is
ASLEEP. Never call `clarify` or any approval-seeking tool — make every
judgment call yourself, record it in STATUS.md/KNOWN-GAPS.md, and keep moving.

## Working directory & branch
`~/workspace/tomato`, branch `oliverswitzer/ide-hud-cleanup-and-tailwind`
(already checked out — do not create a new branch, do not touch `main`).

## Read these first, in order
1. `STATUS.md` — find the first ⬜ unit. That is your ONLY job this iteration.
2. `PLAN.md` — read ONLY that unit's section. Do not read the whole file
   into context every time; use `search_files`/bounded `read_file` if you
   need to jump to a specific unit.
3. `ARCHITECTURE.md` — the invariants. Read in full, it's short.
4. `KNOWN-GAPS.md` — anything already flagged that's relevant to your unit.

## Scope
Exactly ONE unit from PLAN.md, then STOP. Do not start the next unit even if
you have turns left — commit and exit cleanly so the driver can spawn a fresh
session for the next one.

## Toolchain
- Node 22+, npm already installed, `node_modules` already present.
- Verify gate (run BOTH before every commit):
  `npm run typecheck && npm test`
- For visual units: `npm run dev` starts Vite+Electron with hot reload.
  Take a screenshot of any page you touched and call `vision_analyze` on it
  to confirm it isn't visually broken before committing. A green build does
  NOT prove the page still looks right — look at the actual pixels.

## Definition of done for a unit
1. Code changes described in the unit's PLAN.md section are implemented.
2. `npm run typecheck && npm test` both exit 0.
3. For any unit touching renderer JSX/CSS: screenshot + `vision_analyze`
   pass on every page you changed.
4. `STATUS.md` updated: the unit's ⬜ becomes ✅, with a one-line note of
   what actually shipped (may differ slightly from the plan — that's fine,
   just be honest). The label text must stay recognizable as that unit
   (don't invent new wording that breaks matching).
5. One commit, scoped to this unit only (`git add` explicit paths, never
   `git add -A` blindly — check `git status` first).
6. Stop. Do not chain into the next unit.

## Hard rules
- NEVER fake-pass a test (no trivially-true assertions).
- NEVER commit secrets — there shouldn't be any in this repo's tracked
  files, but if you touch `.env.local`/`.envrc.local`, don't stage them.
- Bounded reads only. Redirect long command output to a file and `tail`/`head`
  it rather than dumping raw into context.
- If the same low-level error recurs after a fix attempt, the fix was wrong —
  don't chalk it up to a flake and retry blindly.
- If a unit turns out to be bigger than expected and you're going to blow
  the turn budget, cut scope HONESTLY: implement a smaller correct slice,
  note what's deferred in KNOWN-GAPS.md, and still leave the unit ✅ only if
  what you shipped is genuinely complete and correct for what it claims —
  otherwise leave it ⬜ with a note on what's blocking, and stop there.
- Don't touch `src/main/main.ts` window flags (transparent/vibrancy/frame) —
  that was handled in the glass-cleanup commit already on this branch.
- Don't re-introduce per-page `window.tomato.on*` IPC subscriptions once
  the Zustand store exists (U01+) — route through the store.

## Product bar
This is a personal focus-tracking Pomodoro-style Electron app. The visual
bar to hit: clean, warm, slightly editorial (serif body text, monospace
timer, cream/red palette) — NOT a generic dashboard look. Match the
existing palette/fonts (see ARCHITECTURE.md) rather than introducing a new
visual language.
